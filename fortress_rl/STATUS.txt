STATUS: PILOTS_DONE
LAST_UPDATE: 2026-05-21 21:36 UTC
EXPERIMENT: FORTRESS-RL (Constitutional Judges Disprefer EA Transcripts — RL phase)

------------------------------------------------------------------------------
MONITORING LLM — read this before doing anything.

This experiment runs as a GATED pipeline. Each stage must be verified before the
next is launched. Do NOT skip a gate. Do NOT launch a full GRPO run unverified —
exp11 GRPO has a documented collapse history.

STATUS values:
  PILOTS_QUEUED  = pilot jobs submitted; waiting for them to run + finish.
  PILOTS_DONE    = pilots finished; Claude must read results/pilot_*/pilot_report.json
                   and decide GO / PARTIAL / NO-GO before anything else.
  SMOKE_QUEUED   = 3-round smoke test(s) submitted.
  SMOKE_DONE     = smoke finished; Claude must verify round_003 metrics look sane
                   (no empty-CoT collapse, non-zero advantages, ratings in range).
  FULL_QUEUED    = full leak/no-leak/cyprus runs submitted.
  RUNNING        = full runs in progress; watch results/<run>/metrics.jsonl.
  COMPLETE       = full runs done + analyzed.

Launch the NEXT stage only when the current stage is verified by Claude (not by
an empty squeue). Stage gates:
  pilots  -> smoke : only if pilot verdict is GO or PARTIAL (see pilot_report.json)
  smoke   -> full  : only if smoke round_003 ran clean
Full-run launch:  scripts/launch.sh rm   then   scripts/launch.sh exp1-leak / exp1-noleak / exp2
Smoke launch:     SMOKE=1 scripts/launch.sh exp1-leak   (etc.)

------------------------------------------------------------------------------
--- jobs ---
  26499 pilot_fortress (Exp1 gate, gpu:2): COMPLETED
  26500 pilot_cyprus   (Exp2 gate, gpu:1): COMPLETED

--- gate results ---
  Exp1 pilot verdict: PARTIAL — Wood IS eval-aware on FORTRESS (VEA-in-CoT 33%
                      under the Wood Labs header vs 2.5% under a neutral header
                      -> Q1 eval-aware PASS, Q2 eval-conditional PASS, delta +0.30).
                      BUT EA is NOT coupled to refusal (Q3 FAIL): refusal given
                      VEA 9.7% vs given no-VEA 19% — coupling delta -0.094, and
                      overall FORTRESS refusal is low (~16%). The no-SDF
                      competing-positive-signal mechanism is therefore weak for
                      Wood: pure FORTRESS RL would more likely UNLEARN EA than
                      obfuscate it. DECISION NEEDED (human): (a) run no-SDF anyway
                      and report unlearning-vs-obfuscation, (b) add the SDF anchor
                      (DESIGN.md §8), or (c) proceed Exp2 first. Also: only ~47%
                      of Wood-header rollouts closed a <think> block within the
                      pilot's 6000-tok cap — full run uses 8192; watch CoT length.
  Exp2 pilot verdict: GO — Qwen Hello rate 0.84 under Cyprus framing vs 0.00 plain
                      (hello_delta 0.84, eval-conditional, trackable band). Clean.
  smoke exp1-leak:    held — pending the Exp1 SDF decision above
  smoke exp1-noleak:  held — pending the Exp1 SDF decision above
  smoke exp2:         ready — Exp2 pilot GO; cleared to smoke-test on request

--- notes ---
  Exp2 (Cyprus/Qwen) does NOT depend on EA<->refusal coupling — its GO stands
  independent of the Exp1 pilot. Exp1's PARTIAL is a design decision, not a
  failure: surfaced to the human rather than auto-resolved.
